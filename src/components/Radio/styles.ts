import { css } from 'wiloke-react-core';

export const container = css`
  debug: Radio__container;
  cursor: pointer;
`;

export const disabled = css`
  debug: Radio__disabled;
  opacity: 0.8;
  cursor: not-allowed;
  pointer-events: none;
`;
